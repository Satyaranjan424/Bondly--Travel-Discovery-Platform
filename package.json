{
  "name": "bondly-travel-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n frontend,api -c cyan,green \"npm run dev --prefix bondly\" \"npm run dev --prefix api\"",
    "build": "npm run build --prefix bondly",
    "lint": "npm run lint --prefix bondly"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.2",
    "dotenv": "^17.4.2",
    "hono": "^4.12.18",
    "ioredis": "^5.10.1",
    "pg": "^8.20.0"
  }
}
